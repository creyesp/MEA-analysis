[EXP]
name=MR-0000

[PROJECT]
path=

[FILES]
raw=${PROJECT:path}data/raw/${EXP:name}_analog.mcd
sorting=${PROJECT:path}data/sorting/${EXP:name}.result.hdf5
processed=${PROJECT:path}data/processed_protocols/${EXP:name}_${PROCESSED:suffix}_analysis_of_protocols.hdf5
wn_stim_raw=${PROJECT:path}data/stim/wn/stim_mini_${EXP:name}_.mat
wn_stim=${PROJECT:path}data/stim/wn/stim_mini_${EXP:name}.hdf5

[SYNC]
folder=${PROJECT:path}data/sync/${EXP:name}
events=${SYNC:folder}/event_list_${EXP:name}.csv
wn=${SYNC:folder}/checkerboard.txt
repeated=${SYNC:folder}/repeated_frames_${EXP:name}.txt

[PROCESSED]
suffix=

[REPORT]
sta=${PROJECT:path}report/sta/
sorting=${PROJECT:path}report/fig/sorting_features/

[CHECKERBOAR]
nsamples_before=30
nsamples_after=0
fps=59.75818